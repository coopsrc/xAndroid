gradle.ext.xandroidRoot = settingsDir

def modulePrefix = ":"
if (gradle.ext.has('xandroidModulePrefix')) {
    modulePrefix += gradle.ext.exoplayerModulePrefix
}

include modulePrefix + 'demos'

rootProject.name = 'xAndroid'

apply from: 'core_settings.gradle'